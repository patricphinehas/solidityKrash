pragma solidity ^0.5.0;
// arithmetic operator

// comparison opertor

// operands

// operators

// operations

contract operations{
    function add() public view returns(uint){
        uint a=2;
        uint b=12;
        return b + b++ + a++;
    }
}