pragma solidity >=0.7.0 <0.9.0;
//function to open door and close viz remote control
contract LearnFunction{
//function function-name(parameter list) scope returns(){
//statements
//}
    uint a = 45;
    function openDoor(bool stwitchStatus) public returns(bool){

    }
    //create function that can add two variables
    function addVals() public view returns(uint){
        uint a=9;
        uint b=10;
        uint result=a+b;
        return result;
    }
}